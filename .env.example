DATABASE_URL=postgresql://username:password@ip-address/database-name
# Optional for docker-compose.prod.yml (overrides DATABASE_URL inside containers)
# DATABASE_URL_CONTAINER=postgresql://username:password@ip-address/database-name
# POSTGRES_HOST=postgres
# POSTGRES_USER=app
# POSTGRES_PASSWORD=app
# POSTGRES_DB=boilerplate
BETTER_AUTH_SECRET=xxx # generate using `openssl rand -hex 32`
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000
SERVER_API_URL=http://localhost:4000
CORS_ORIGIN=http://localhost:3000
